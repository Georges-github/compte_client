{% extends 'base.html.twig' %}

{% block title %}Identification{% endblock %}

{% block body %}

<form method="post">

    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

    {# <div class="container-fluid">

        <div class="row">

            <div class="col-12 col-md-6 mx-auto border border-3 rounded-5 p-5">

                <div class="fs-5 fw-light d-flex justify-content-md-center mb-3">Identification</div>

                <div class="mb-3">
                    <label for="inputCourriel" class="form-label">Courriel</label>
                    <input type="email" value="{{ last_username }}" name="courriel" id="inputCourriel" class="form-control">
                </div>

                <div class="mb-3">
                    <label for="inputPassword" class="form-label">Mot de passe</label>
                    <input type="password" class="form-control" id="inputPassword">
                </div>

                <div>
                    <label class="mb-3">
                        <input type="checkbox" name="_remember_me" class="mb-3" checked>
                        Rester connecté
                    </label>
                </div>

                <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                >

                <button type="submit" class="btn btn-outline-light btn-sm w-25 rounded-3">Valider</button>

            </div>

        </div>

    </div> #}

        <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
        <label for="username">Courriel</label>
        <input type="text" value="{{ last_username }}" name="courriel" id="username" class="form-control" autocomplete="username" required autofocus>
        <label for="password">Password</label>
        <input type="password" name="password" id="password" class="form-control" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Remember me</label>
            </div>
        #}

        <button class="btn btn-lg btn-primary" type="submit">
            Sign in
        </button>

</form>
{% endblock %}
