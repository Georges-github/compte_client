<form method="post" action="{{ path('app_etat_contrat_delete', {'id': etat_contrat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat_contrat.id) }}">
    <button class="btn">Delete</button>
</form>
